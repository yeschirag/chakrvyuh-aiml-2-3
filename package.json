{
  "name": "fullstack-app",
  "private": true,
  "scripts": {
    "build": "cd web && npm install && npx vite build && cd ../server && npm install",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": "20.x"
  }
}
