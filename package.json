{
  "name": "fullstack-app",
  "private": true,
  "scripts": {
    "build": "cd web && npm ci && npm run build && cd ../server && npm ci",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": "20.x"
  }
}